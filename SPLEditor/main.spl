mod Add(a, b);
       return a + b;
modEnd();

mod multi(a, b);
       return a * b;
modEnd();

// Entery point
mod main(); 
     dec_i x = Add(2, 3);
     dec_i age = multi(2,  34);
     dec_s name = "Martyn";
     pout "x = " + x;
     pout "Age is " + age;
     pout "Hi " + name + " Your age is: " + age;
     pout "Do loop";
 dec_i j = 0;
do( j; j isLess 100; j++ ) {
   pout j;
};

pout "change to while loop";
dec_i i = 20;
while( i isMore 10 ) {
   i--;
   pout i;
};

modEnd();



